<%= page_title 'hub.start.title' %>
<% content_for :body_classes, 'hub-start' %>
<% content_for :feedback_source, 'START_PAGE' %>

<h1 class="heading-large"><%= t 'hub.start.heading' %></h1>

<%= form_for @form, url: start_path, html: {id: 'start-page-form', class: 'js-validate', novalidate: 'novalidate'} do |f| %>

  <% if flash[:errors] %>
    <div id="validation-error-message-no-js" class="validation-message form-group">
      <p class="error"><%= flash[:errors] %></p>
    </div>
  <% end %>

  <%= content_tag :div, class: flash[:errors] ? "form-group error" : "form-group" do %>
    <fieldset>
      <%= render 'shared/block_label', form: f, key: :selection, value: true, label: t('hub.start.answer_yes'), attributes: {required: true, data: { msg: t('hub.start.error_message')}} %>
      <%= render 'shared/block_label', form: f, key: :selection, value: false, label: t('hub.start.answer_no'), attributes: {required: true, data: { msg: t('hub.start.error_message')}} %>
    </fieldset>
  <% end %>

  <div id="validation-error-message-js"></div>

  <div class="form-group-tight">
    <div class="actions">
      <input class="button" id="next-button" type="submit" value="<%= t 'hub.start.continue' %>" />
    </div>
  </div>
<% end %>
