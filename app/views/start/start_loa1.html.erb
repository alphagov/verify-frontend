<%= page_title 'hub.start_loa1.title' %>
<% content_for :body_classes, 'hub-start' %>
<% content_for :feedback_source, 'START_PAGE' %>

<!--TODO: Fix translations-->

<p><%= t 'hub.start_loa1.verify_is_a_service' %></p>
<%= @tailored_text.html_safe %>

<div class="actions">
  <%= button_link_to t('hub.start_loa1.get_set_up_with_gov_uk_verify'), begin_registration_path, id: 'begin-registration-link' %>
  <div>
    <%= t 'hub.start_loa1.or' %>
    <%= link_to t('hub.start_loa1.sign_in_with_existing'), begin_sign_in_path, id: 'begin-sign-in-link'  %>
  </div>
</div>

